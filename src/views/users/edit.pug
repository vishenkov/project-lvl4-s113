extends /layouts/application.pug

block content
  +formFor(f, { url: urlFor('editUser'), query: '?_method=patch', method: 'post' })
    +input(f, 'firstName', { labelName: 'First name' })
    +input(f, 'lastName', { labelName: 'Last name' })
    +button(f)

  a.nav-link(href=urlFor('resetPswdUser')) Reset Password
  a.nav-link(href=urlFor('deleteUser')) Delete User
