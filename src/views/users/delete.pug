extends /layouts/application.pug

block content
  +formFor(f, { url: urlFor('deleteUser'), method: 'delete' })
    +input(f, 'password', { as: 'password', labelName: 'Please, enter the password' })
    +button(f, { value: 'Delete' })