extends /layouts/application.pug

block content
  table.table.table-stripped
    thead
      tr
        th Full Name
        th Email
        th Created At
    tbody
      each user in users
        tr
          td= user.fullName
          td
            a.nav-link(href=urlFor('profile', user.id))= user.email
          td= user.createdAt
