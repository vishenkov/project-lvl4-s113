extends /layouts/application.pug

block content
  table.table.table-stripped
    thead
      tr
        th Name
        th Created At
    tbody
      each tag in tags
        tr
          td
            a.nav-link(href=urlFor('tag', tag.id))= tag.name
          td= tag.createdAt
